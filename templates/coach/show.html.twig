{% extends "back/base.html.twig" %}

{% block body %}
    <h1>Coach</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ coach.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ coach.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ coach.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ coach.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_coach_index') }}">back to list</a>

    <a href="{{ path('app_coach_edit', {'id': coach.id}) }}">edit</a>
    <a href="{{ path('app_coach_delete', {'id': coach.id}) }}">edit</a>


    {# {{ include('coach/_delete_form.html.twig') }} #}
{% endblock %}
