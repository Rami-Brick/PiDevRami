{% extends "back/base.html.twig" %}

{% block body %}
    <h1>Academy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ academy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ academy.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ academy.category }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_academy_index') }}">back to list</a>

    <a href="{{ path('app_academy_edit', {'id': academy.id}) }}">edit</a>

    {{ include('academy/_delete_form.html.twig') }}
{% endblock %}
